# CLAUDE.md

## Bootstrap Rule

For any non-trivial task, start with `AI_CONTEXT.md`.

Use this reading chain:

1. `CLAUDE.md`
2. `AI_CONTEXT.md`
3. `docs/context/project-overview.md`
4. `docs/context/retrofit-mapping.md`{{retrofitReadNote}}
5. `docs/context/development-roadmap.md`{{roadmapReadNote}}
6. `docs/context/architecture.md`
7. `docs/context/tech-stack.md`
8. the relevant `docs/tasks/<module>/INDEX.md` files for the task
9. relevant files under `docs/engineering/`
10. relevant files under `docs/tasks/`

## Purpose

This repository uses layered project context so a new AI conversation can recover the whole project meaning without scanning the whole repo.

- `AI_CONTEXT.md` is the compressed bootstrap index.
- `docs/context/` stores stable project meaning.
- `docs/engineering/` stores technical contracts.
- `docs/tasks/<module>/INDEX.md` stores the evolving state summary for each module.
- `docs/tasks/` stores module-local execution context.
- `docs/archive/` stores historical docs and superseded project materials.

## Project Notes

- Project: `{{projectName}}`
- Shape: `{{shape}}`
- Execution workflow: `{{executionWorkflow}}`
- Product goal: {{idea}}
- Core flow: {{coreFlow}}

## Maintenance Rule

When project structure or canonical project understanding changes, update `AI_CONTEXT.md`, the affected `docs/tasks/<module>/INDEX.md` files, and the relevant context docs together.

## Repository Structure Priority

- Generic workflow tooling must adapt to this repository's structure, not the other way around.
- Do not create generic skill artifact paths such as `docs/superpowers/**` when canonical repository paths already exist.
- Use:
  - `docs/context/` for stable project context
  - `docs/product/` for product intent
  - `docs/engineering/` for engineering contracts
  - `docs/tasks/` for task and module records
  - `docs/archive/` for historical docs, older project materials, and superseded planning artifacts
- Keep repository-level rules generic and intentional; do not inherit personal conventions from unrelated source repositories by accident.

## Mandatory Writeback

- `docs/tasks/<module>/INDEX.md` files are the core module-state files for this repository.
- After each completed task, the AI must write back the latest effective status, notable shipped changes, and next-task implications to the affected module `INDEX.md` files.
- A single task should minimize its module impact whenever possible. Prefer work that stays within one module instead of spreading changes across unrelated modules.
- If a task genuinely crosses module boundaries, update every affected module `INDEX.md` so the repository state stays accurate.
- If historical execution docs or older project materials are no longer active, move them under `docs/archive/` instead of leaving them mixed into active context.

## Superpowers Mapping

{{superpowersArtifactMapping}}

## Skill Routing

{{skillRouting}}
